```markdown
# 第二十二届智能车电磁组训练任务三（软件部分）

## 理论学习

### Git

Git 作为开发中最常用的版本控制工具，在编写代码时也需要根据团队协作与项目管理要求掌握其核心原理。在后续和队友的合作与代码管理 Git 都是很好的工具。

### 串口

1. 推荐大家在调试代码过程中充分利用串口对程序运行状态进行追踪，自行学习串口的使用方法，推荐：VOFA+（其他也可以，不作硬性要求）
2. 了解板载 ADC XPT2046 的使用方法。

## 实践任务

本次考核任务需要实现简单虚拟示波器功能，将 ADC 采集数据通过串口发送给电脑，使用上位机软件显示出波形。可以充分利用板载硬件对 ADC 功能进行测试。（**需要通过 Git 来进行版本管理**）

## 基础要求

1. 读取 ADC 数据并通过串口发送给电脑上位机（选择自己喜欢的上位机即可，对软件不作要求），并显示出波形。（验收时会接信号发生器）
2. 添加逻辑代码，实现基于中断接收方式的串口数据通信，用于修改变量的值。如：上位机发送 `Value = 150`（不含引号），将下位机中对应变量的值修改为 150。

## 进阶要求

1. 使用单片机一引脚输出频率和占空比可调的方波，用于验证 ADC 功能。
2. 读取 ADC 多个通道的数据并在上位机同时显示。
```